# Personal resume data — stays local
resume-base.md
resume-base-old-*.md
resumes/
applications.json

# Resume files (personal)
*.docx
*.pdf
Jason English Resume*

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# NOTE: docx-template.js is now committed (see commit history). It was previously
# gitignored, but that let branch switches / auto-stashes and stray re-inits
# clobber it with broken versions. Keep it tracked. Re-run /init-docx-template
# only if your .docx formatting template actually changes, then commit the result.

# Temp generation scripts
generate_resume.js
generate_resumes.js

# Temp docx build artifacts
docx_scripts/
template_unpacked/

# Node
node_modules/

# Claude local session permissions (machine-specific)
.claude/settings.local.json

# Editor
.vscode/
